
public function Main() -> void
{
    print(fibonacci(Fibonacc));
}

public function Fibonacci(n: int) -> int
{
    if (n <= 1) 
    {
        return n;
    }
    return fibonacci(n - 2) + fibonacci(n - 1);
}
